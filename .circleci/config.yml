#version: 2
#jobs:
#  build:
#    docker: # See https://docs.docker.com/get-started/#docker-concepts if you are new to Docker.
#      - image: circleci/ruby:2.4.1-jessie
#    steps:
#      - checkout
#      - run: echo "A first hello"
version: 2
jobs:
  build:
    #...
  deploy:
    machine:
      enabled: true
    steps:
      - run:
          name: Deploy Over SSH
          command: |
            ssh ec2-user@54.169.205.222 "touch a"

#workflows:
#  version: 2
#  build-and-deploy:
#    jobs:
#      - build
#      - deploy:
#          requires:
#            - build
#          filters:
#            branches:
#              only: master
